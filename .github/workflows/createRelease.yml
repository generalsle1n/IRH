name: Create Release
on:
  workflow_call:
    inputs:
      nextVersion:
        type: string
        required: true
      binaryPatternName:
        type: string
        required: true
  
jobs:
  createRelease:
    name: Create Release
    
    permissions: 
      contents: write
    
    runs-on: self-hosted
    
    steps:
      - name: Download Setup Files
        uses: actions/download-artifact@v7
        with:
          pattern: ${{ env.BinaryPatternName }}
      
      - name: CreateRelease
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.nextVersion }}
          generate_release_notes: true
          make_latest: true
          files: ${{ inputs.binaryPatternName }}

      - name: Delete Artifacts
        uses: geekyeggo/delete-artifact@v5
        with:
          name: ${{ inputs.binaryPatternName }}

